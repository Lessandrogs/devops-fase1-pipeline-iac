{
  "name": "demo-ci-iac",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint src __tests__ --ext .js --max-warnings=0",
    "test": "jest --coverage",
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); require('fs').writeFileSync('dist/index.html','ok');\"",
    "start": "node src/index.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest": { "testEnvironment": "node" },
  "jest-junit": { "output": "app/junit.xml" }
}
