{
  "name": "demo-ci-iac",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "jest --coverage",
    "build": "echo 'build placeholder' && mkdir -p dist && echo 'ok' > dist/index.html",
    "start": "node src/index.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  }
}
